{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ventilationformulaire</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ventilationFormulaire.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ventilationformulaire_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ventilationformulaire_edit', { 'id': ventilationFormulaire.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
